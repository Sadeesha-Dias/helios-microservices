server:
  port: 8083
spring:
  application:
    name: fraud
  datasource:
     username: helios
     url: jdbc:postgresql://localhost:5432/fraud
     password: password
  jpa:
     hibernate:
       hbm2ddl:
         auto: update
     properties:
       hibernate:
         dialect: org.hibernate.dialect.PostgreSQLDialect
         format_sql: true
     show-sql: true
